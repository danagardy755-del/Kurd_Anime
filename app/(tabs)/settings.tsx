import { StyleSheet, Text, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { useColors } from "@/hooks/use-colors";
import { STREAM_API_URL, STREAM_SETUP_NOTE } from "@/lib/catalog";
export default function SettingsScreen() { const colors = useColors(); return <ScreenContainer className="p-5"><Text style={[styles.title, { color: colors.foreground }]}>ڕێکخستنەکان</Text><View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[styles.cardTitle, { color: colors.foreground }]}>سەرچاوەی کاتەلۆگ</Text><Text style={[styles.text, { color: colors.muted }]}>Jikan API بۆ metadata بەکاردێت.</Text><Text selectable style={[styles.url, { color: colors.primary }]}>{STREAM_API_URL}</Text></View><View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[styles.cardTitle, { color: colors.foreground }]}>Stream</Text><Text style={[styles.text, { color: colors.muted }]}>{STREAM_SETUP_NOTE}</Text></View></ScreenContainer>; }
const styles = StyleSheet.create({ title: { fontSize: 28, fontWeight: "900", marginBottom: 18 }, card: { borderRadius: 16, borderWidth: 1, padding: 16, marginBottom: 12 }, cardTitle: { fontWeight: "900", fontSize: 16, marginBottom: 8 }, text: { fontSize: 13, lineHeight: 21 }, url: { fontSize: 11, marginTop: 10 } });
